This is docker-compose file for WordPress with MariaDB.

## start docker
```bash
$ docker-compose up -d
```

And access `http://localhost:8000` with your browser.

## stop docker
```bash
$ docker-compose down
```
